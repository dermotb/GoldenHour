<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="GoldenHour.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GoldenHour"
	xmlns:c="using:GoldenHour.Common"
    xmlns:data="using:GoldenHour.Data"
    xmlns:common="using:GoldenHour.Common"
	xmlns:bm="using:Bing.Maps"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	
	<Page.Resources>
		<c:ShortDateConverter x:Key="ShortDateConverter"/>
	</Page.Resources>
	
	<!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid Style="{StaticResource LayoutRootStyle}" >
		<Grid.Background>
			<ImageBrush x:Name="backgroundGrid" ImageSource="/Assets/Turks.jpg" Stretch="Fill"/>
		</Grid.Background>
		<Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
			<Grid Grid.Column="1" Margin="10,20,10,10">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<StackPanel Grid.Row="0">
					<TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" IsHitTestVisible="false" Style="{StaticResource PageHeaderTextStyle}"/>
					<TextBlock x:Name="status" Text="" IsHitTestVisible="false" Foreground="Yellow" FontSize="15" FontWeight="Normal"/>
				</StackPanel>
				<StackPanel  Grid.Row="0" Grid.Column="1" Orientation="Vertical">
					<TextBox Text="{Binding ElementName=DatePicker, Path=SelectedDate, Converter={StaticResource ShortDateConverter}, Mode=OneWay}" IsEnabled="False" Width="200" Margin="20"/>
					<c:DateSpinner x:Name="DatePicker" Width="600" Height="100"/>
				</StackPanel>
			</Grid>
        </Grid>

		<Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}" Grid.Row="2" Margin="0, 20, 0,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="400" />
			</Grid.ColumnDefinitions>
			<Grid Grid.Column="1">
				<StackPanel>
					<TextBlock x:Name="accTitle" Text="Location accuracy (metres):" IsHitTestVisible="false" Foreground="WhiteSmoke" FontSize="20" FontWeight="Bold"/>
					<TextBlock x:Name="accuracy" Text="999" IsHitTestVisible="false" Foreground="WhiteSmoke" FontSize="20"/>
					<TextBlock Margin="0,10,0,0" x:Name="locTitle" Text="Location:" IsHitTestVisible="false" Foreground="WhiteSmoke" FontSize="20" FontWeight="Bold"/>
					<TextBlock x:Name="latText" Text="999" TextAlignment="Left" IsHitTestVisible="false"  Foreground="WhiteSmoke" FontSize="20"/>
					<TextBlock x:Name="longText" Text="999" TextAlignment="Left" IsHitTestVisible="false" Foreground="WhiteSmoke" FontSize="20" />
					<TextBlock Margin="0,10,0,0" x:Name="srTitle" Text="Sunrise" IsHitTestVisible="false" Foreground="WhiteSmoke" FontSize="20" FontWeight="Bold"/>
					<TextBlock x:Name="sunRise" Text="999" IsHitTestVisible="false" Foreground="WhiteSmoke" FontSize="20"/>
					<TextBlock Margin="0,10,0,0" x:Name="ssTitle" Text="Sunset" IsHitTestVisible="false" Foreground="WhiteSmoke" FontSize="20" FontWeight="Bold"/>
					<TextBlock x:Name="sunSet" Text="999" IsHitTestVisible="false" Foreground="WhiteSmoke" FontSize="20" />
					<Button Content="Reset Location" HorizontalAlignment="Left" Margin="0,0,0,0" Click="CurrentCalc_Click"/>
				</StackPanel>
	
			</Grid>
			<Grid x:Name="mapParent" Grid.Column="0">
				<bm:Map x:Name="map"  Credentials="AnjGi7VxPGjgcCSQ_Y68JePAkmxdfjlFB3GfsJNRzmRxA-WeATMqUrUSJ8iG4C84"  RightTapped="map_RightTapped_1" PointerPressedOverride="map_PointerPressedOverride"/>
			</Grid>
		</Grid>

		<VisualStateManager.VisualStateGroups>

            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape"/>
                <VisualState x:Name="Filled"/>

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="itemGridView" Storyboard.TargetProperty="Padding">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="96,136,86,56"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!--
                    The back button and title have different styles when snapped, and the list representation is substituted
                    for the grid displayed in all other view states
                -->
                <VisualState x:Name="Snapped">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pageTitle" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedPageHeaderTextStyle}"/>
                        </ObjectAnimationUsingKeyFrames>

                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="itemListView" Storyboard.TargetProperty="Visibility">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="Visible"/>
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="itemGridView" Storyboard.TargetProperty="Visibility">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="Collapsed"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>
